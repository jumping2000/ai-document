-- AI Document Platform — PostgreSQL init
CREATE EXTENSION IF NOT EXISTS "uuid-ossp";
CREATE EXTENSION IF NOT EXISTS "pg_trgm";

-- Indexes created by Alembic migrations, but ensure the DB and extensions exist
